<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <title>Ditt konto</title>
  <link rel="stylesheet" href="/css/theme.css">
  <link rel="stylesheet" href="/css/layout.css">
  <style>
    .account-container {
      max-width: 500px;
      margin: 3rem auto;
      padding: 2rem;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      text-align: center;
    }

    .account-container img {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 1rem;
    }

    .account-container h2 {
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
    }

    .account-container p {
      font-size: 1rem;
      color: #666;
    }

    .account-container .back-link {
      margin-top: 2rem;
      display: inline-block;
      color: var(--primary-color);
    }
  </style>
</head>
<body>
  <div class="account-container">
    <img src="<%= user.profileImageUrl || '/default-profile.png' %>" alt="Profilbild">
    <h2><%= user.name || 'Namnlös användare' %></h2>
    <p><%= user.email %></p>

    <a href="/" class="back-link">← Tillbaka till startsidan</a>
  </div>
</body>
</html>
